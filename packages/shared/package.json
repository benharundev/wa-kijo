{
  "name": "@wa-kijo/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Zod schemas and types shared between api and web — populated in Phase 2",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo 'No build needed for source-shared package.'",
    "test": "echo 'No tests yet.' && exit 0",
    "lint": "echo 'No lint config yet.' && exit 0",
    "typecheck": "echo 'Nothing to typecheck yet.' && exit 0"
  }
}
